define(["xstyle!./style.css"], function(require, exports, module) {
    var $ = require("jquery");
    $(document).on('cloudcms-ready', function(event) {
        // append new option to select dropdown

        // when click on new option fetch the old node, store in variable

        // then fetch the new node, store in variable

        // use difftool to compare the two variables

        // open new tab with the contents of the two docs
    });
});