define(function(require, exports, module) {
  //detect if current page is page edit properties (extend to handle descendents)
  //if not, do nothing and exit
  
  console.error("PAGE  BOZ")
  //inject button next to URL field
  //Query for other pages (or other types defined to check for) with URL set to the current value
  //If any are found, indicate invalid
  //Provide links to other pages?
});
